'use client'
// Canonical Optimize savings-report path. Renders the same SavingsView that
// /dashboard/savings renders (legacy URL preserved).
import { SavingsView } from '../../_components/SavingsView'

export default function OptimizeSavingsReportPage() {
    return <SavingsView />
}
